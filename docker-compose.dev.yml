# Development overlay: mounts .cursor for NDJSON debug logging.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  gateway:
    volumes:
      - ./.cursor:/app/.cursor
