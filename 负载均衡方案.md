# CrabCache 负载均衡优化建议

> 基于对以下核心模块的静态审计：`crab-route/ring.rs`、`crab-proxy/upstream_pool.rs`、`crab-proxy/runtime.rs`、`crab-proxy/proxy.rs`、`crab-gateway/config.rs`

---

## 架构速览

```
客户端请求
  │
  ▼
GatewayProxy::request_filter()   ← Auth / Rate Limit / Body parse / Affinity key 提取
  │
  ▼
GatewayProxy::upstream_peer()    ← AffinityRouter::select_healthy() 选后端节点
  │
  ▼
GatewayProxy::upstream_request_filter() ← 注入 upstream API Key（Bearer）
  │
  ▼
DeepSeek / 其他 LLM 上游
  │
  ▼
GatewayProxy::upstream_response_filter() ← record_success / record_failure → 熔断
```

**两层负载均衡**：
1. **后端节点层**（`AffinityRouter` / `pingora_ketama`）：一致性哈希，决定流量打到哪个 IP:Port
2. **API Key 层**（`UpstreamKeyPool`）：最小飞行请求数（Least-inflight）+ RR 混合，决定用哪个 API Key

---

## 问题一：熔断器与 `UpstreamKeyPool` 相互独立，缺乏联动

### 现状

- 后端节点的健康状态存放在 `RuntimeConfig::backend_health`（`IndexMap<String, BackendHealth>`）
- `UpstreamKeyPool` 的冷却逻辑（`cooldown_until_ms`）完全独立
- 当某后端节点进入 `CircuitState::Open` 后，API Key 层的飞行计数仍然照常增加，`acquire()` 不感知后端状态

### 影响

- 流量已因熔断被路由到其他节点，但原节点的 `inflight` 计数不会归零，导致后续 `HalfOpen` 期间选键决策偏斜
- `rotate_after_rate_limit()` 只按 `account_id` 隔离，不按后端节点隔离，两套逻辑互不知情

### 建议

将 `UpstreamKeyGuard` 绑定到后端名称，使 `acquire()` 支持可选的后端过滤：

```rust
// upstream_pool.rs 新增
pub fn acquire_for_backend(
    self: &Arc<Self>,
    excluded_backend: Option<&str>,   // Open 的后端 → 跳过与之绑定的 key
) -> Option<UpstreamKeyGuard> { ... }
```

或者在 `upstream_peer()` 中，当所有可选后端均不健康时提前返回 503，避免进入 `upstream_request_filter()`。

---

## 问题二：`select_healthy` 降级逻辑破坏粘性路由语义

### 现状

```rust
// ring.rs:196-200
if healthy_count > 0 {
    for b in &self.backends {
        if is_healthy(&b.name) {
            return Some(b.as_ref()); // 线性扫描，非哈希顺序
        }
    }
}
```

当一致性哈希选中的主节点不健康时，降级路径是**按 `Vec` 顺序**线性扫描，而不是按哈希环的"下一节点"顺序。

### 影响

- 破坏 DeepSeek 的 **Prompt Cache（KV Cache）亲和性**：同一 conversation/user 理应粘到哈希环的相邻节点，线性扫描会让所有溢出流量都涌向 `backends[0]`，KV Cache 命中率骤降
- 加剧节点宕机时的流量不均：只有 `backends[0]` 承压，`backends[1]`、`backends[2]` 空载

### 建议

使用 `pingora_ketama::Continuum` 的多次 `node()` 查找（虚节点遍历）而不是线性扫描：

```rust
// ring.rs — 改进 select_healthy
pub fn select_healthy<F>(&self, key: &[u8], is_healthy: F) -> Option<&Backend>
where
    F: Fn(&str) -> bool,
{
    // 按哈希环顺序尝试，最多遍历 backends.len() 次
    // pingora_ketama Continuum 暂不提供 iterator，需要哈希偏移模拟
    let n = self.backends.len();
    for attempt in 0..n {
        // 在 key 末尾追加 attempt 作为"虚节点跳"
        let mut probe = key.to_vec();
        probe.extend_from_slice(&(attempt as u32).to_le_bytes());
        if let Some(addr) = self.continuum.node(&probe) {
            if let Some(b) = self.backends.iter().find(|b| b.addr == addr)
                && is_healthy(&b.name)
            {
                return Some(b.as_ref());
            }
        }
    }
    // 最终降级：任意健康节点（保持现有行为）
    self.backends.iter().find(|b| is_healthy(&b.name)).map(|b| b.as_ref())
}
```

> [!NOTE]
> 若 `pingora_ketama` 后续支持 `nodes_for_key()` 迭代器，则可直接使用，无需上述 hack。

---

## 问题三：熔断器 `success_threshold=2` 不适合 LLM 长流

### 现状

```rust
// ring.rs:47-54
impl Default for CircuitBreakerConfig {
    fn default() -> Self {
        Self {
            failure_threshold: 5,
            success_threshold: 2,  // ← 2 次成功即关闭
            timeout_ms: 30_000,
        }
    }
}
```

`record_success` 在 `upstream_response_filter()` 中被调用（HTTP 200 时）。LLM 流式响应可能持续数分钟，`HalfOpen` 状态下如果两个并发流都在进行，计数会竞争加速关闭。

### 影响

- `half_open_successes` 使用 `u32`（非原子操作），由于写锁保护尚可，但逻辑上对 SSE 流的"成功"定义不清：收到 `200 OK` 头就算成功，还是流结束才算？目前是前者（头部到达即触发）
- 对于不稳定的上游，`HalfOpen → Closed → Open` 抖动频率高

### 建议

1. **区分"响应头成功"与"流完成成功"**：在 `logging_phase()` 中（流结束后）再 `record_success`，而不是在 `upstream_response_filter()` 的头部阶段
2. **提高 `success_threshold`**（建议 3–5）或通过配置暴露：

```toml
# config.toml
[upstream]
circuit_breaker_failure_threshold = 5
circuit_breaker_success_threshold = 3
circuit_breaker_timeout_secs = 30
```

---

## 问题四：`UpstreamKeyPool::acquire()` 存在 TOCTOU 竞态

### 现状

```rust
// upstream_pool.rs:344-352
let inflight = slot.inflight.load(Ordering::Relaxed);
if inflight < best_inflight {
    best_inflight = inflight;
    best_idx = Some(i);
}
// ...之后...
let inflight = self.slots[idx].inflight.fetch_add(1, Ordering::Relaxed) + 1;
```

"读最小飞行数 → 选最优 slot → 原子 +1"之间，其他线程可能已修改 inflight，导致选出的 slot 已不再是最优解。

### 影响

在高并发场景（数百个 Tokio 任务同时调用 `acquire()`），多个任务可能同时读到同一 slot 的最小 inflight，全部选同一 slot，导致短时流量集中。

### 建议

对于 LLM 网关这类场景，TOCTOU 影响可接受（最终一致），但若有多账号场景，可改为 **compare-and-swap 自旋**：

```rust
// 对 best_idx 的 inflight 做 CAS 确认仍是最优，否则重试
// 或使用 fetch_add + 后校验（pessimistic）
let prev = self.slots[idx].inflight.fetch_add(1, Ordering::AcqRel);
// prev 远超预期时 fetch_sub 回退并重试其他 slot
```

> [!TIP]
> 当前实现对于 key 数量 ≤ 10、并发 ≤ 200 的场景已经足够好，优先解决以下优先级更高的问题。

---

## 问题五：`rr` 计数器溢出后行为

### 现状

```rust
// upstream_pool.rs:325
let start = self.rr.fetch_add(1, Ordering::Relaxed) % n;
```

`AtomicUsize` 溢出后自动 wrapping（`usize::MAX + 1 = 0`），但 `% n` 仍正确，无实际 bug。

**但**：`hot_replace()` 时复制了旧 `rr` 值：

```rust
rr: AtomicUsize::new(old.rr.load(Ordering::Relaxed)),
```

若旧池有 5 个 slot，`rr = 3`，新池有 3 个 slot，`3 % 3 = 0` 仍然正确。无问题。

### 结论

此处**无需修改**。

---

## 问题六：健康检查是被动式，缺少主动探测

### 现状

`BackendHealth` 的 `record_failure` / `record_success` 完全依赖**真实流量**触发，config 中有 `health_check_interval_secs = 30`，但在代码中**未见主动健康检查任务**：

```rust
// config.rs:326
#[serde(default = "default_health_check_interval_secs")]
pub health_check_interval_secs: u64,
```

搜索代码库，`health_check_interval_secs` 只在配置解析中出现，无对应的后台任务。

### 影响

- 低流量时段（深夜 0 请求/分钟），`Open` 的后端只能靠 `timeout_ms = 30s` 超时后转 `HalfOpen`，无法主动发现恢复
- 冷启动时如果上游临时不可达，所有后端可能在前几个请求后全部 Open，直到 30s 后才能尝试恢复

### 建议

添加后台健康探测任务（轻量级 HEAD 请求）：

```rust
// crab-gateway/src/main.rs 或独立模块
tokio::spawn(async move {
    let mut interval = tokio::time::interval(
        Duration::from_secs(health_check_interval_secs)
    );
    loop {
        interval.tick().await;
        for backend in router.backends() {
            let ok = probe_backend(&backend).await;
            let mut health = backend_health.write();
            let h = health.entry(backend.name.clone())
                .or_insert_with(BackendHealth::new_healthy);
            if ok { h.record_success(&circuit_cfg); }
            else   { h.record_failure(&circuit_cfg); }
        }
    }
});
```

---

## 问题七：Affinity Key 提取在 `request_filter` 和 `upstream_peer` 中各执行一次

### 现状

```rust
// proxy.rs:657-662 (request_filter)
ctx.upstream.affinity_key = Some(extract_affinity_key(...));

// proxy.rs:1478-1479 (upstream_peer)
let affinity_key = extract_affinity_key(...); // 重新计算！
```

`extract_affinity_key` 包含 SHA-256 哈希（`sha2::Sha256`），每次调用都会分配并哈希数据。

### 建议

在 `upstream_peer()` 中优先使用已存的 `ctx.upstream.affinity_key`：

```rust
// upstream_peer
let affinity_key = ctx.upstream.affinity_key
    .clone()
    .unwrap_or_else(|| extract_affinity_key(&headers, &client_ip, body_pck, ctx.project_id.as_deref()));
```

---

## 优先级矩阵

| # | 问题 | 影响范围 | 实现难度 | 优先级 |
|---|------|----------|----------|--------|
| 6 | 缺少主动健康检查 | 稳定性 | 中 | **P0** |
| 2 | `select_healthy` 降级破坏 KV Cache 亲和性 | KV Cache 命中率 / 负载均衡 | 中 | **P1** |
| 1 | 熔断与 Key 池缺乏联动 | 故障隔离 | 中 | **P1** |
| 3 | 熔断 `success_threshold` 过低 + 触发时机早 | 稳定性 | 低 | **P2** |
| 7 | Affinity Key 重复计算 | CPU（微小） | 低 | **P2** |
| 4 | `acquire()` TOCTOU 竞态 | 负载均衡精度 | 高 | **P3** |
| 5 | `rr` 溢出 | 无实际问题 | — | 不需处理 |

---

## 决策摘要

- **Mode**: standard（生产服务）
- **Edition**: 2024
- **Rules Applied**: P0（正确性/稳定性）、P1（KV Cache 亲和性对业务有直接影响）
- **Conflicts Resolved**: None
- **Deviations**: None
- **Trade-offs**: 主动健康检查会增加对上游的额外连接压力，建议用 HEAD 而非 GET，且仅在检测到 Open 状态的后端时才主动探测（按需触发）
