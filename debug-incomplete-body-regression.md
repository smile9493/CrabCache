# Debug Session: incomplete-body-regression
- **Status**: [OPEN]
- **Issue**: Gateway still returns `incomplete_body` after the non-streaming defer stop-gap, request id `2ec5834e-3694-4ef9-a52d-125f52e64880`.
- **Scope**: Reconfirm whether the failing request is still entering defer, whether it is `stream=true`, and whether the deployed binary includes the latest guard.

## Hypotheses & Verification
| ID | Hypothesis | Likelihood | Evidence Needed |
|----|------------|------------|-----------------|
| A | The failing request is `stream=true`, so it still uses the defer path and hit the old tail-loss bug | High | Debug A/B/E points for this `request_id` |
| B | `wuming` is not running the latest gateway binary, so `non_streaming_request` guard never took effect | High | Gateway SHA / latest debug logs / trace fields |
| C | The request is `stream=false`, but some alternate path still arms defer before the guard | Medium | `streaming_defer_reject_reason` + debug arm logs |
| D | The error is generated by the gateway finalize guard, not by the upstream provider | High | Match error string to local source and request trace |
| E | A second truncation bug exists on the streaming defer path even after the non-streaming hotfix | Medium | `append_tail_bytes`, `assembled_len`, `session.is_body_done`, request_body_filter entry logs |

## Immediate Checks
1. Confirm the error payload is local gateway output, not provider-native output.
2. Pull debug NDJSON for `request_id=2ec5834e-3694-4ef9-a52d-125f52e64880`.
3. Verify whether `stream=true/false` and whether defer armed for this request.
